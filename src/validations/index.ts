import * as authSchema from "./auth.validation";
import * as configSchema from "./config.validation";
import * as userSchema from "./user.validation";
import * as memoSchema from "./memo.validation";

import * as groupSchema from "./group.validation";
import * as topicSchema from "./topic.validation";

import * as tagSchema from "./tag.validation";
import * as workerSchema from "./worker.validation";
import * as botSchema from "./bot.validation";

import * as mailSchema from "./mail.validation";

export { authSchema, configSchema, userSchema, memoSchema, groupSchema, topicSchema, tagSchema, workerSchema, botSchema, mailSchema };
